import el from '../utils/elements'
//import uid from '../utils/uid'

export default {
	init() {
		if($(el("form", true, "--conversational")).length) {
			//
		}
	},
	navigation() {
	},
	legend() {
	},
};